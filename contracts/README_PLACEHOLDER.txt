PlonkVerifier.sol is NOT included in this repo tree by hand -- it is
generated by `bash scripts/setup-zkey.sh` (part of `bash setup.sh`), which
runs `snarkjs zkey export solidityverifier` against your compiled circuit's
proving key. It will appear here after you run setup. This placeholder file
is deleted automatically the first time you run setup.
